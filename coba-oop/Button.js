class Button {
  constructor(type, text, color) {
    this.type = type
    this.text = text
    this.color = color
  }
}

let shareButton = new Button("share", "Share", "orang")
let slideShowButton = new Button("actionable", "Slideshow", "white")